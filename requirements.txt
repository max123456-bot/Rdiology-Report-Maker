streamlit>=1.41
python-docx>=1.1.0
pypdf>=4.2.0

# The AI engine: layout classification, OCR, dictation, house-style drafting.
google-genai>=1.0.0

# Postgres. Required when STORAGE_URL points at a database, which it must on any
# host with an ephemeral filesystem (Streamlit Cloud wipes it on every restart).
psycopg[binary]>=3.1

# Audio conversion for the AI4Bharat speech engine. Needs ffmpeg, which
# packages.txt installs on Linux hosts.
pydub>=0.25.1

# DICOM header reading, for the report-vs-scan cross-check. Pure Python.
pydicom>=2.4

# DICOM networking: C-ECHO/C-FIND against a PACS, and the C-STORE receiver
# modalities push to on the clinic-LAN install. Pure Python.
pynetdicom>=2.0

# Scan clean-up before OCR: CLAHE, bilateral denoise, deskew, adaptive
# threshold. Headless build - no GUI libraries dragged in.
opencv-python-headless>=4.8

# Free local OCR tier. Needs the tesseract binary too: packages.txt installs
# tesseract-ocr on Streamlit Cloud; Windows uses the UB Mannheim installer.
# The app degrades to Gemini Vision when either piece is absent.
pytesseract>=0.3.10

# Modality rules (rules_schema.yaml) and structured output schemas.
pyyaml>=6.0
pydantic>=2.0

# The service layer: uvicorn api:app - the engine over REST for RIS
# integration. The Streamlit UI does not use these.
fastapi>=0.110
uvicorn>=0.29

# ---------------------------------------------------------------------------
# Optional, left out on purpose
#
# docx2pdf     PDF export. Needs Microsoft Word installed, so it is Windows/macOS
#              only and pointless on a Linux host.
# torch        Running AI4Bharat locally instead of via Hugging Face. Gigabytes,
#              and far past what a free host will allocate.
# transformers
